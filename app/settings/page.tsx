import { SettingsForm } from '@/components/forms/settings';

const Settings = async () => {
  return (
    <main className="w-full max-w-xl mx-auto mt-20 space-y-6">
      <SettingsForm />
    </main>
  );
};

export default Settings;
